# Railway Nixpacks Configuration
# 耀聖藥局智慧排班系統

[phases.setup]
  nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
  cmds = ["pnpm install"]
  dependsOn = ["setup"]

[phases.build]
  cmds = ["pnpm run build"]
  dependsOn = ["install"]

[start]
  cmd = "pnpm run start"
  dependsOn = ["build"]
