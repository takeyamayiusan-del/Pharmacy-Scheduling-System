# Railway Nixpacks Configuration
# 耀聖藥局智慧排班系統（統一使用 npm）

[phases.setup]
  nixPkgs = ["nodejs_20"]

[phases.install]
  cmds = ["npm ci"]
  dependsOn = ["setup"]

[phases.build]
  cmds = ["npm run build"]
  dependsOn = ["install"]

[start]
  cmd = "npm start"
  dependsOn = ["build"]
