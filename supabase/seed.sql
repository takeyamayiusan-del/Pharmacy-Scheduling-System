-- ============================================================
-- 耀聖藥局智慧排班系統 - Seed Data
-- ============================================================

-- ============================================================
-- 1. 預設排班規則（僅一筆）
-- ============================================================
INSERT INTO public.scheduling_rules (
  monthly_leave_quota,
  saturday_leave_quota,
  weekday_leave_quota,
  min_evening_staff
) VALUES (
  8,   -- 每月 8 天休假（4 固定週日 + 2 週六 + 2 平日）
  2,   -- 週六排休上限 2 天
  2,   -- 平日排休上限 2 天
  2    -- 晚班最低人數 2 人
);

-- ============================================================
-- 2. 初始員工資料（佔位說明）
-- ============================================================
-- 注意：以下員工帳號需先在 Supabase Auth 建立對應的 auth.users 記錄，
-- 才能插入 public.users（因為 id 為 FK 參照 auth.users.id）。
--
-- 員工名單：
--   店長：佾珊（manager，帳號密碼登入）
--   員工：宜孝（employee，無密碼登入）
--   員工：貞葶（employee，無密碼登入）
--   員工：聖文（employee，無密碼登入，特殊排班規則）
--   員工：桂香（employee，無密碼登入）
--
-- 建立方式（透過 Supabase Dashboard 或 Admin API）：
--   1. 在 Supabase Auth 為每位使用者建立帳號
--      - 店長使用真實 email + 密碼
--      - 員工使用虛擬 email（如 yixuan@yaosheng.internal）+ 隨機密碼
--   2. 取得各使用者的 UUID，填入下方 INSERT 語句
--
-- 範例（請替換 UUID 為實際值）：
-- INSERT INTO public.users (id, name, role, is_active) VALUES
--   ('00000000-0000-0000-0000-000000000001', '佾珊', 'manager',  TRUE),
--   ('00000000-0000-0000-0000-000000000002', '宜孝', 'employee', TRUE),
--   ('00000000-0000-0000-0000-000000000003', '貞葶', 'employee', TRUE),
--   ('00000000-0000-0000-0000-000000000004', '聖文', 'employee', TRUE),
--   ('00000000-0000-0000-0000-000000000005', '桂香', 'employee', TRUE);
